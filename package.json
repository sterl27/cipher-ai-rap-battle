{
    "name": "musaix-pro",
    "version": "1.0.0",
    "description": "Musaix Pro — An agentic, modular ecosystem for professional music production and AI-driven audio workflows",
    "scripts": {
          "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
          "frontend": "cd frontend && nuxt dev",
          "backend": "cd backend && node server.js",
          "build": "npm run build --prefix frontend",
          "generate": "npm run generate --prefix frontend",
          "start": "npm run preview --prefix frontend"
    },
    "dependencies": {
          "concurrently": "^8.0.0"
    },
    "devDependencies": {},
    "engines": {
          "node": ">=18.0.0"
    }
}
